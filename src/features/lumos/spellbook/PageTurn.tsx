/**
 * PageTurn（Phase 17.1-C Step 1 占位）。
 *
 * 定位：翻页动画逻辑组件。
 *   - 点击羽毛笔 / 右页右下角 → 翻页动画
 *   - 右页 rotateY 0° → -180°（翻向左）
 *   - 翻页完成后更新当前研究索引
 *
 * Step 1 范围：占位，不渲染任何内容。
 */
export default function PageTurn() {
  // TODO Step 5: 实现翻页动画（rotateY + 内容切换）
  return null;
}
