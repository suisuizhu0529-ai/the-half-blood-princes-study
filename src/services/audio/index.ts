/**
 * Audio service entry point.
 *
 * Re-exports the singleton AudioManager instance so consumers
 * can import from a single, stable path.
 */

export { audioManager, AudioManager } from "./audioManager";
